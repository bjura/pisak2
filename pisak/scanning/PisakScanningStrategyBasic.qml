import QtQuick 2.5


/*!
    \qmltype PisakScanningStrategyBasic
    \brief Basic scanning strategy.

    Currently it is the default scanning strategy and does
    not extend the base class in any way.
*/
PisakScanningStrategy {
}
